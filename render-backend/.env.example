# ── Local Development ──
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/proctorai

# ── Render (auto-injected via render.yaml) ──
# DATABASE_URL is set automatically by Render when you link the PostgreSQL addon
