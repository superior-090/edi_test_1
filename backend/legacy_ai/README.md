# Legacy AI scripts

These files came from the old standalone local Python backend. They are kept for reference only.

The active backend is `backend/app/main.py`, started with:

```powershell
python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```
